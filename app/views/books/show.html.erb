<!-- 更新・削除処理の成功後、サクセスメッセージを表示、テキストの色指定なし（黒） -->
<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<h2 class="book-box">
  <p>Title:<%= @book.title %></p>
</h2>

<h2 class="book-box">
  <p>Body:<%= @book.body %></p>
</h2>

<%= link_to "Edit", edit_book_path(@book.id) %> | <%= link_to "Back", books_path %>